'use strict';

exports.systemConfigCheck = function(systemConfig) {
    var res = [];
    
    // if (systemConfig === undefined) {
        // res.push({type: 'error', message: 'Config file: "config/system_init.json" not found'});
    // }
    
    return res;
};

